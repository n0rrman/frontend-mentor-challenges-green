import { Overpass, Ubuntu } from "next/font/google";

export const overpass = Overpass({ subsets: ["latin"], weight: ["300", "600"] });
export const ubuntu = Ubuntu({ subsets: ["latin"], weight: ["400", "500", "700"], });
