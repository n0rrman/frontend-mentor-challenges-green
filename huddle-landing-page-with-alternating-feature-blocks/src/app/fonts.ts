import { Open_Sans } from "next/font/google";
import { Poppins } from "next/font/google";

export const headingFont = Poppins({ subsets: ["latin"], weight: ["600"] });
export const mainFont = Open_Sans({ subsets: ["latin"], weight: ["400", "700"] });
